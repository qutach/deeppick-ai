# Example Dockerfile for custom database image (PostgreSQL)
FROM postgres:15

# Optional: Add custom initialization scripts
# COPY ./init.sql /docker-entrypoint-initdb.d/


# No custom commands needed for standard PostgreSQL
