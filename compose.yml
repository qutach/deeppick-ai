services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: deeppick_ai_frontend
    ports:
      - "8080:8080"
    env_file:
      - .env
    command: ["python", "main.py"]